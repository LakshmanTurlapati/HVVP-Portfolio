---
phase: 04-polish-launch
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - harsha-portfolio/.vercel (generated by Vercel CLI, not checked in)
autonomous: false
requirements:
  - POL-07
  - POL-04
  - POL-05

must_haves:
  truths:
    - "next build completes with no errors and no TypeScript failures"
    - "The site is live at a public Vercel URL"
    - "Lighthouse desktop LCP is under 2.5s"
    - "All external links (email mailto, LinkedIn, resume PDF) work in production"
    - "The skip-to-content link and focus rings work in the deployed site"
  artifacts:
    - path: "harsha-portfolio/.next"
      provides: "Production build output"
      contains: "BUILD_ID"
  key_links:
    - from: "Vercel deployment"
      to: "harsha-portfolio/ directory"
      via: "vercel --yes from project root"
      pattern: "vercel"
    - from: "Lighthouse"
      to: "deployed URL"
      via: "manual run or CLI"
      pattern: "LCP"
---

<objective>
Validate production build, deploy to Vercel, and confirm the live site meets the LCP under 2.5s target with all links working correctly.

Purpose: Ship the portfolio to a public URL so it can be shared with recruiters.
Output: Live Vercel deployment URL with passing build and verified external links.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-polish-launch/04-01-SUMMARY.md
@harsha-portfolio/package.json

<interfaces>
<!-- Vercel CLI commands for deployment -->
<!-- Run all commands from inside harsha-portfolio/ directory -->

Build command: npm run build
  - Runs: next build
  - Expect output: "Route (app) / Static" -- confirms static generation
  - Watch for: any TypeScript errors, missing module errors

Vercel CLI (install if needed):
  npm install -g vercel
  vercel --version

Deploy commands:
  cd harsha-portfolio && vercel --yes
  # --yes accepts defaults: framework=Next.js, build=npm run build, output=.next
  # First run: prompts for login (checkpoint), then deploys
  # Subsequent runs: uses cached project config

After deploy, Vercel outputs a preview URL like: https://harsha-portfolio-xxxx.vercel.app

Production promotion:
  vercel --prod
  # Promotes to production alias

Links to verify in production:
  - mailto:harshavprabu@gmail.com  (click Email Me button)
  - https://www.linkedin.com/in/hveeravalliprabu  (click LinkedIn button)
  - /resume.pdf  (click Download Resume button -- must download, not 404)
  - Skip-to-content link (tab once from page load)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production build validation</name>
  <files>
    harsha-portfolio/.next (build output, not committed)
  </files>
  <read_first>
    - harsha-portfolio/package.json (confirm build script)
    - harsha-portfolio/node_modules/next/dist/docs/01-app/02-guides/index.md (skim for static generation notes)
  </read_first>
  <action>
    Run the production build from inside the harsha-portfolio directory:
    ```
    cd /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio && npm run build
    ```

    Expected successful output includes:
    - "Compiled successfully" or similar
    - Route table showing "/" as Static (circle icon, not lambda)
    - No TypeScript errors
    - No "Module not found" errors
    - Build size for the main page JS bundle -- note this in the summary

    If build FAILS:
    - Read the error message carefully
    - Common issues and fixes:
      a. "Cannot find module 'motion/react'" -- motion is already in package.json, this should not occur. If it does, run: npm install
      b. TypeScript errors in AnimatedSection.tsx -- fix the specific type error reported
      c. "use client" boundary issues -- if a Server Component tries to import a Client Component that re-exports server-only code, isolate the boundary
      d. Missing public files (resume.pdf, og-image.png) -- these are placeholders from Phase 1 and are expected to be present

    If build SUCCEEDS: Note the total JS bundle size for First Load JS reported for the "/" route. If it exceeds 150kB (gzipped), investigate what is inflating the bundle -- motion tree-shaking is expected to keep animated code minimal.

    Do NOT start the server (per CLAUDE.md -- never run applications automatically).
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>
    - npm run build completes with exit code 0
    - Route "/" shows as Static in the build output
    - No TypeScript or module errors in build output
    - First Load JS size noted in summary
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy to Vercel and verify live site</name>
  <files>harsha-portfolio/.vercel</files>
  <action>
    Run Vercel CLI deployment from inside harsha-portfolio directory:
    ```
    cd /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio
    npx vercel --yes 2>&1
    ```
    If Vercel CLI requests browser authentication, open the URL shown in the terminal.
    After successful deploy, also run: npx vercel --prod
    to promote to the production alias.
  </action>
  <verify>
    <automated>MISSING -- deployment URL must be confirmed by human after Vercel CLI output</automated>
  </verify>
  <what-built>
    Vercel deployment of the portfolio. Claude will run the Vercel CLI to deploy. This checkpoint pauses for you to:
    1. Authenticate with Vercel if prompted (browser-based OAuth)
    2. Confirm the deployment completed and provide the live URL
    3. Manually verify the live site

    Before this checkpoint, Claude will attempt:
    ```
    cd /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio
    npx vercel --yes 2>&1
    ```
    If Vercel CLI requests browser authentication, the terminal will show a URL to open.
  </what-built>
  <how-to-verify>
    After deployment completes and you have the live URL, verify the following:

    1. Open the live URL in your browser -- the portfolio should load with all 6 sections visible.

    2. Scroll down the page -- each section should fade in as it enters the viewport.

    3. Test scroll animations: sections should animate in. Resize browser to mobile (375px) and confirm layout.

    4. Test external links:
       - Click "Email Me" -- should open your email client with harshavprabu@gmail.com as recipient
       - Click "LinkedIn" -- should open https://www.linkedin.com/in/hveeravalliprabu in a new tab
       - Click "Download Resume" in the hero -- should download a PDF (or show 404 if resume.pdf was a placeholder)

    5. Test keyboard accessibility:
       - Press Tab on page load -- a "Skip to main content" link should appear in the top-left
       - Press Tab again -- focus should move to the first Navbar link with a visible blue outline
       - Continue tabbing through all nav links, CTA buttons -- all should show visible focus rings

    6. Run Lighthouse on the live URL (Chrome DevTools > Lighthouse > Desktop):
       - LCP should be under 2.5s
       - Accessibility score should be 100 (or note any issues)
       - Performance score should be 90+

    If Lighthouse shows LCP above 2.5s, the most likely causes are:
    - Font blocking (Inter is loaded with display:swap, should be fine)
    - Large hero paint (text-only hero, should be fast)
    - Motion library initial load (tree-shaken, should be minimal)
    Report the actual LCP value so it can be noted in the summary.
  </how-to-verify>
  <done>
    - Site is live at a public Vercel URL
    - All external links work in production
    - Lighthouse LCP under 2.5s confirmed
    - Focus rings and skip-to-content link confirmed working
  </done>
  <resume-signal>
    Type the live Vercel URL and confirm: "deployed" if all links work, or describe any issues found.
    Example: "https://harsha-portfolio-abc.vercel.app -- deployed, all links work, LCP 1.8s"
  </resume-signal>
</task>

</tasks>

<verification>
Build validation:
```
cd /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio && npm run build 2>&1 | grep -E "error|Error|Static|Dynamic|compiled"
```
Must show successful compilation with "/" as Static.

Deployment verification (manual -- see checkpoint task above).

Post-deploy link checks (manual in browser):
- Email CTA opens mailto correctly
- LinkedIn opens in new tab
- Resume PDF downloads (not 404)
- Skip-to-content link visible on first Tab press
- Focus rings visible on all interactive elements
</verification>

<success_criteria>
- npm run build exits with code 0, "/" route is Static
- Site is live at a public Vercel URL (provided in checkpoint response)
- Lighthouse desktop LCP under 2.5s
- All external links (email, LinkedIn, resume PDF) function correctly in production
- Skip-to-content and focus rings confirmed working in browser
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-launch/04-02-SUMMARY.md` using the summary template.
Include the live Vercel URL in the summary.
</output>
