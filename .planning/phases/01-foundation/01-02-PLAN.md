---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - harsha-portfolio/src/app/globals.css
  - harsha-portfolio/src/app/layout.tsx
  - harsha-portfolio/src/components/layout/Footer.tsx
  - harsha-portfolio/src/components/layout/Navbar.tsx
autonomous: true
requirements:
  - DES-01
  - DES-02
  - DES-03
  - DES-04
  - POL-02
  - POL-03
  - POL-06

must_haves:
  truths:
    - "Inter font is loaded via next/font/google and applied to the entire page via --font-inter CSS variable on <html>"
    - "The page background is the off-white color defined by --color-background token (oklch(0.99 0.00 0))"
    - "One muted blue accent color token --color-accent exists and generates bg-accent, text-accent, border-accent utility classes"
    - "Browser tab title shows 'Harsha Vardhini Veeravalli Prabu | Digital Marketing Professional'"
    - "Page source contains <meta name='description' content='Data-driven digital marketing professional"
    - "Page source contains og:title and og:description meta tags for LinkedIn sharing"
    - "Page source contains a <script type='application/ld+json'> tag with '@type': 'Person' schema"
    - "Layout HTML contains <header>, <main>, and <footer> landmark elements"
  artifacts:
    - path: "harsha-portfolio/src/app/globals.css"
      provides: "Tailwind v4 design tokens via @theme"
      contains: "--color-background"
    - path: "harsha-portfolio/src/app/layout.tsx"
      provides: "Root layout with Inter font, SEO metadata, JSON-LD, semantic HTML"
      exports: ["metadata", "RootLayout"]
    - path: "harsha-portfolio/src/components/layout/Footer.tsx"
      provides: "Minimal footer stub with copyright"
    - path: "harsha-portfolio/src/components/layout/Navbar.tsx"
      provides: "Navbar placeholder stub (empty, Phase 3 will implement)"
  key_links:
    - from: "harsha-portfolio/src/app/layout.tsx"
      to: "harsha-portfolio/src/app/globals.css"
      via: "import './globals.css'"
      pattern: "import './globals.css'"
    - from: "harsha-portfolio/src/app/globals.css"
      to: "--font-inter CSS variable"
      via: "--font-sans: var(--font-inter)"
      pattern: "var(--font-inter)"
    - from: "harsha-portfolio/src/app/layout.tsx"
      to: "next/font/google Inter"
      via: "inter.variable on <html> className"
      pattern: "inter.variable"
---

<objective>
Wire the Tailwind v4 design system (globals.css @theme tokens), load Inter font via next/font/google, write the full root layout.tsx with SEO metadata export and JSON-LD Person schema, and create the semantic HTML landmark structure with placeholder Navbar and minimal Footer stubs.

Purpose: This plan establishes the visual foundation and SEO surface that every Phase 2 section component will inherit. Requirements DES-01 through DES-04 (design system) and POL-02, POL-03, POL-06 (SEO + semantics) are all satisfied here.
Output: A globals.css with design tokens, a complete layout.tsx, a Footer stub, and a Navbar placeholder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Tailwind v4 @theme pattern from RESEARCH.md. Use exactly these token names. -->
<!-- globals.css @theme block: -->
```css
@import "tailwindcss";

@theme {
  --font-sans: var(--font-inter), ui-sans-serif, system-ui, sans-serif;
  --color-background: oklch(0.99 0.00 0);
  --color-foreground: oklch(0.15 0.01 260);
  --color-accent: oklch(0.55 0.10 240);
  --color-accent-hover: oklch(0.48 0.12 240);
  --color-muted: oklch(0.93 0.00 0);
  --color-muted-foreground: oklch(0.50 0.01 260);
  --color-card: oklch(0.97 0.00 0);
  --radius-sm: 0.25rem;
  --radius-md: 0.5rem;
  --radius-lg: 0.75rem;
}
```

<!-- next/font/google Inter pattern from RESEARCH.md: -->
```typescript
import { Inter } from 'next/font/google'
const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
  display: 'swap',
})
// Usage on <html>: className={inter.variable}
```

<!-- SEO metadata from RESEARCH.md (exact values to use): -->
```typescript
export const metadata: Metadata = {
  metadataBase: new URL('https://harshavardhini.com'),
  title: 'Harsha Vardhini Veeravalli Prabu | Digital Marketing Professional',
  description: 'Data-driven digital marketing professional specializing in SEO, social media, and campaign optimization. University Gold Medalist. MS Marketing at UT Dallas.',
  openGraph: {
    title: 'Harsha Vardhini | Digital Marketing Professional',
    description: 'SEO, SEM, social media, and campaign optimization specialist. Open to full-time marketing roles in the US.',
    url: 'https://harshavardhini.com',
    siteName: 'Harsha Vardhini Portfolio',
    locale: 'en_US',
    type: 'website',
    images: [{ url: '/og-image.png', width: 1200, height: 630, alt: 'Harsha Vardhini Veeravalli Prabu -- Digital Marketing Professional' }],
  },
  twitter: { card: 'summary_large_image', title: 'Harsha Vardhini | Digital Marketing Professional', description: 'SEO, SEM, and campaign optimization specialist. MS Marketing at UT Dallas.' },
  alternates: { canonical: 'https://harshavardhini.com' },
}
```

<!-- JSON-LD Person schema exact values from RESEARCH.md: -->
```typescript
const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'Person',
  name: 'Harsha Vardhini Veeravalli Prabu',
  url: 'https://harshavardhini.com',
  email: 'harshavprabu@gmail.com',
  jobTitle: 'Digital Marketing Professional',
  description: 'Data-driven digital marketing professional specializing in SEO, social media, and campaign optimization.',
  alumniOf: [
    { '@type': 'CollegeOrUniversity', name: 'The University of Texas at Dallas' },
    { '@type': 'CollegeOrUniversity', name: 'University of Madras' },
  ],
  knowsAbout: ['SEO', 'SEM', 'Social Media Marketing', 'Campaign Optimization', 'Google Analytics', 'HubSpot', 'Meta Ads Manager', 'Shopify', 'SEMrush'],
  sameAs: ['https://www.linkedin.com/in/hveeravalliprabu'],
}
// Render: <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd).replace(/</g, '\\u003c') }} />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write globals.css with Tailwind v4 @theme design tokens</name>
  <files>harsha-portfolio/src/app/globals.css</files>
  <read_first>
    - /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio/src/app/globals.css (read current state — shadcn may have added @layer base variables)
    - /Users/lakshmanturlapati/Documents/Codes/Harsha/.planning/phases/01-foundation/01-RESEARCH.md (lines 163-204 for exact @theme pattern, lines 501-530 for pitfall avoidance)
  </read_first>
  <action>
    Rewrite /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio/src/app/globals.css with the following content.

    CRITICAL rules:
    - The `@import "tailwindcss"` MUST be the first line
    - All custom design tokens (colors, font, radius) MUST go in the `@theme {}` block, NOT in `:root` and NOT in `@layer base`
    - `scroll-behavior: smooth` goes in bare `html {}` CSS after the theme block (not in @theme, not in @layer base)
    - `body {}` base styles go in bare CSS (not in @theme)
    - If shadcn added a `@layer base { :root { ... } }` block with variables like `--background`, `--foreground`, etc., KEEP it after your @theme block -- those are shadcn component tokens and must not be removed
    - Do NOT use `@tailwind base`, `@tailwind components`, `@tailwind utilities` (those are v3 directives)

    Final file content structure:
    ```css
    @import "tailwindcss";

    @theme {
      /* Typography */
      --font-sans: var(--font-inter), ui-sans-serif, system-ui, sans-serif;

      /* Colors */
      --color-background: oklch(0.99 0.00 0);
      --color-foreground: oklch(0.15 0.01 260);
      --color-accent: oklch(0.55 0.10 240);
      --color-accent-hover: oklch(0.48 0.12 240);
      --color-muted: oklch(0.93 0.00 0);
      --color-muted-foreground: oklch(0.50 0.01 260);
      --color-card: oklch(0.97 0.00 0);

      /* Border radius */
      --radius-sm: 0.25rem;
      --radius-md: 0.5rem;
      --radius-lg: 0.75rem;
    }

    html {
      scroll-behavior: smooth;
    }

    body {
      background-color: var(--color-background);
      color: var(--color-foreground);
    }

    /* [shadcn @layer base block here if it exists -- preserve as-is] */
    ```

    Write the exact content above, preserving any existing shadcn @layer base block that was already present.
  </action>
  <verify>
    <automated>grep -n "@theme" /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio/src/app/globals.css && grep -n "color-background" /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio/src/app/globals.css && grep -n "color-accent" /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio/src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - globals.css line 1 is `@import "tailwindcss";`
    - globals.css contains `@theme {` block
    - globals.css contains `--font-sans: var(--font-inter), ui-sans-serif, system-ui, sans-serif;`
    - globals.css contains `--color-background: oklch(0.99 0.00 0);`
    - globals.css contains `--color-foreground: oklch(0.15 0.01 260);`
    - globals.css contains `--color-accent: oklch(0.55 0.10 240);`
    - globals.css contains `--color-muted-foreground:`
    - globals.css contains `scroll-behavior: smooth;`
    - `grep -c "@tailwind base" /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio/src/app/globals.css` outputs `0` (no v3 directives)
  </acceptance_criteria>
  <done>globals.css defines all design tokens in @theme block, scroll-behavior smooth set, body base styles applied</done>
</task>

<task type="auto">
  <name>Task 2: Write layout.tsx with Inter font, SEO metadata, JSON-LD, and semantic HTML</name>
  <files>
    harsha-portfolio/src/app/layout.tsx,
    harsha-portfolio/src/components/layout/Footer.tsx,
    harsha-portfolio/src/components/layout/Navbar.tsx
  </files>
  <read_first>
    - /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio/src/app/layout.tsx (read current state generated by create-next-app)
    - /Users/lakshmanturlapati/Documents/Codes/Harsha/.planning/phases/01-foundation/01-RESEARCH.md (lines 584-689 for complete layout.tsx example, lines 292-341 for JSON-LD inline script pattern)
  </read_first>
  <action>
    Write three files as described below. Use the exact values specified -- do not use placeholders.

    --- FILE 1: src/app/layout.tsx ---

    Replace the existing layout.tsx entirely with the following implementation.

    Exact requirements:
    - Import `Inter` from `'next/font/google'` with `subsets: ['latin']`, `variable: '--font-inter'`, `display: 'swap'`
    - Export `const metadata: Metadata` with the exact title, description, openGraph, twitter, and alternates values from the interfaces block above
    - Declare `const jsonLd` object with exactly the schema values from the interfaces block (name, url, email, jobTitle, description, alumniOf, knowsAbout, sameAs)
    - The `<html>` element must have: `lang="en"` AND `className={inter.variable}`
    - The `<head>` element (inside `<html>`) must contain the JSON-LD script: `<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd).replace(/</g, '\\u003c') }} />`
    - The `<body>` element must have `className="bg-background text-foreground font-sans"`
    - Inside `<body>`: `<header>` (imports Navbar stub), then `<main>{children}</main>`, then `<footer>` (imports Footer stub)
    - Import Navbar from `'@/components/layout/Navbar'` and Footer from `'@/components/layout/Footer'`
    - The file must be a Server Component (no 'use client' directive)
    - Import `'./globals.css'`

    --- FILE 2: src/components/layout/Navbar.tsx ---

    Create a minimal placeholder stub. This will be fully implemented in Phase 3.
    Content:
    ```typescript
    // Navbar -- Phase 3 implementation
    // Section IDs that nav links will target: hero, about, experience, skills, leadership, contact
    export default function Navbar() {
      return null
    }
    ```

    --- FILE 3: src/components/layout/Footer.tsx ---

    Create a minimal footer with copyright. Use only Tailwind utility classes.
    Content:
    ```typescript
    export default function Footer() {
      return (
        <footer className="py-6 text-center text-sm text-muted-foreground border-t border-muted">
          <p>&copy; {new Date().getFullYear()} Harsha Vardhini Veeravalli Prabu</p>
        </footer>
      )
    }
    ```

    NOTE: The Footer component IS a Server Component. Do NOT add 'use client'. The `new Date()` in JSX is fine in RSC -- it evaluates at build time.

    IMPORTANT: Do NOT run `npm run dev`. Do NOT add 'use client' to layout.tsx. Do NOT use `next/head`.
  </action>
  <verify>
    <automated>grep -n "inter.variable" /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio/src/app/layout.tsx && grep -n "application/ld+json" /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio/src/app/layout.tsx && grep -n "Harsha Vardhini Veeravalli Prabu" /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio/src/app/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - layout.tsx contains `import { Inter } from 'next/font/google'`
    - layout.tsx contains `variable: '--font-inter'`
    - layout.tsx contains `className={inter.variable}` on the `<html>` element
    - layout.tsx contains `export const metadata: Metadata`
    - layout.tsx contains `title: 'Harsha Vardhini Veeravalli Prabu | Digital Marketing Professional'`
    - layout.tsx contains `metadataBase: new URL('https://harshavardhini.com')`
    - layout.tsx contains `openGraph:` with `type: 'website'`
    - layout.tsx contains `images: [` inside openGraph (OG image array)
    - layout.tsx contains `type="application/ld+json"`
    - layout.tsx contains `dangerouslySetInnerHTML`
    - layout.tsx contains `replace(/</g, '\\u003c')`
    - layout.tsx contains `'@type': 'Person'`
    - layout.tsx contains `<header>` landmark element
    - layout.tsx contains `<main>` landmark element
    - layout.tsx does NOT contain `'use client'`
    - layout.tsx does NOT contain `import Head from 'next/head'`
    - File /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio/src/components/layout/Navbar.tsx exists
    - File /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio/src/components/layout/Footer.tsx exists
    - Footer.tsx contains `&copy;`
    - Footer.tsx contains `text-muted-foreground` (uses design token class)
  </acceptance_criteria>
  <done>layout.tsx exports metadata with correct SEO values, renders JSON-LD Person schema in head, applies Inter font via CSS variable, and structures body with header/main/footer landmarks</done>
</task>

</tasks>

<verification>
Verify the design system tokens generate utility classes by running TypeScript compilation check:
```bash
cd /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio && npx tsc --noEmit 2>&1 | head -30
```
Expected: zero TypeScript errors in layout.tsx, Footer.tsx, Navbar.tsx.

Verify JSON-LD syntax is valid:
```bash
node -e "
const fs = require('fs');
const content = fs.readFileSync('/Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio/src/app/layout.tsx', 'utf8');
const match = content.match(/const jsonLd = (\{[\s\S]*?\})\s*\nexport/);
console.log('JSON-LD block found:', !!match);
"
```
</verification>

<success_criteria>
- globals.css: @theme block contains 7 color tokens + font-sans + 3 radius tokens
- layout.tsx: metadata export present with title matching 'Harsha Vardhini Veeravalli Prabu | Digital Marketing Professional'
- layout.tsx: JSON-LD script in head with @type Person and email harshavprabu@gmail.com
- layout.tsx: <html> has lang="en" and inter.variable className
- layout.tsx: <body> has bg-background and text-foreground classes
- layout.tsx: semantic structure with <header>, <main>, <footer>
- Footer.tsx: renders copyright with current year
- Navbar.tsx: placeholder stub returns null
- npx tsc --noEmit passes with zero errors across these files
</success_criteria>

<output>
After completion, create `/Users/lakshmanturlapati/Documents/Codes/Harsha/.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
