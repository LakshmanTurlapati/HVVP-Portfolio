---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - harsha-portfolio/src/app/page.tsx
  - harsha-portfolio/public/og-image.png
  - harsha-portfolio/public/resume.pdf
autonomous: false
requirements:
  - DES-04
  - POL-02
  - POL-06

must_haves:
  truths:
    - "page.tsx contains exactly 6 section elements with locked IDs: hero, about, experience, skills, leadership, contact"
    - "Each section element has an aria-label matching its purpose"
    - "public/og-image.png exists and is 1200x630 pixels (required for LinkedIn large card preview)"
    - "public/resume.pdf is present (linked by Hero CTA in Phase 2)"
    - "Running npm run dev renders a blank page with off-white background and no console errors"
  artifacts:
    - path: "harsha-portfolio/src/app/page.tsx"
      provides: "Section ID stubs locking the architecture for Phase 3 Navbar anchor links"
      contains: "id=\"hero\""
    - path: "harsha-portfolio/public/og-image.png"
      provides: "1200x630 OG image for LinkedIn sharing preview"
    - path: "harsha-portfolio/public/resume.pdf"
      provides: "Downloadable resume PDF linked from Hero CTA"
  key_links:
    - from: "harsha-portfolio/src/app/page.tsx"
      to: "Phase 3 Navbar"
      via: "section id attributes (hero, about, experience, skills, leadership, contact)"
      pattern: "id=\"hero\""
    - from: "harsha-portfolio/public/og-image.png"
      to: "harsha-portfolio/src/app/layout.tsx"
      via: "openGraph.images[0].url = '/og-image.png'"
      pattern: "/og-image.png"
---

<objective>
Write the page.tsx section stub skeleton that locks all section IDs for the rest of the project, copy the resume PDF to public/, and add a 1200x630 OG image needed for LinkedIn sharing (POL-02).

Purpose: page.tsx must lock section IDs now -- they are consumed by Phase 3 Navbar anchor links (`href="#hero"`, etc.) and by Phase 3 Intersection Observer. Changing them after Phase 2 would break navigation. The OG image is required for LinkedIn Post Inspector to show a large card format.
Output: page.tsx with 6 section stubs, a 1200x630 og-image.png, and resume.pdf in /public.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md

<interfaces>
<!-- Section IDs are LOCKED after this plan. Do not change them in Phase 2 or 3. -->
<!-- Phase 3 Navbar will use plain <a href="#hero"> anchors (not Next.js <Link>) -->
<!-- per STATE.md: "Use plain <a href='#section'> anchors -- Next.js <Link> suppresses smooth scroll" -->

Locked section IDs:
- hero
- about
- experience
- skills
- leadership
- contact

<!-- Phase 2 section components will replace the stub <section> elements with their full implementations. -->
<!-- The id and aria-label attributes must be preserved exactly as defined in this plan. -->

<!-- layout.tsx expects page.tsx output to render inside <main>{children}</main> -->
<!-- bg-background and text-foreground come from the @theme tokens set in Plan 02 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write page.tsx with locked section ID stubs</name>
  <files>harsha-portfolio/src/app/page.tsx</files>
  <read_first>
    - /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio/src/app/page.tsx (read current create-next-app generated content)
    - /Users/lakshmanturlapati/Documents/Codes/Harsha/.planning/phases/01-foundation/01-RESEARCH.md (lines 456-474 for page.tsx stub pattern and section ID rationale)
  </read_first>
  <action>
    Replace the contents of /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio/src/app/page.tsx entirely with the following:

    ```typescript
    // src/app/page.tsx
    // Section IDs are LOCKED -- do not rename after Phase 2 begins.
    // Phase 3 Navbar uses plain <a href="#[id]"> anchors for smooth scroll.
    // Phase 3 Intersection Observer targets these same IDs for active-link highlighting.

    export default function Home() {
      return (
        <>
          {/* Hero section -- Phase 2: HeroSection component */}
          <section id="hero" aria-label="Hero" className="min-h-screen" />

          {/* About section -- Phase 2: AboutSection component */}
          <section id="about" aria-label="About" />

          {/* Experience section -- Phase 2: ExperienceSection component */}
          <section id="experience" aria-label="Experience" />

          {/* Skills section -- Phase 2: SkillsSection component */}
          <section id="skills" aria-label="Skills" />

          {/* Leadership section -- Phase 2: LeadershipSection component */}
          <section id="leadership" aria-label="Leadership" />

          {/* Contact section -- Phase 2: ContactSection component */}
          <section id="contact" aria-label="Contact" />
        </>
      )
    }
    ```

    This is a Server Component -- do NOT add 'use client'.
    The empty section stubs are intentional. Phase 2 replaces each stub with its full section component.
    The `min-h-screen` on the hero section ensures the page has visible height during Phase 1 development.

    IMPORTANT: Do NOT import any components from src/data/ or src/components/ in this file during Phase 1. Phase 2 will add those imports.
  </action>
  <verify>
    <automated>grep -c "id=\"" /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio/src/app/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx contains `id="hero"` with `aria-label="Hero"`
    - page.tsx contains `id="about"` with `aria-label="About"`
    - page.tsx contains `id="experience"` with `aria-label="Experience"`
    - page.tsx contains `id="skills"` with `aria-label="Skills"`
    - page.tsx contains `id="leadership"` with `aria-label="Leadership"`
    - page.tsx contains `id="contact"` with `aria-label="Contact"`
    - `grep -c "id=\""` outputs `6` (exactly 6 section IDs)
    - page.tsx does NOT contain `'use client'`
    - page.tsx does NOT contain `import` statements for components or data files (Phase 1 is stubs only)
  </acceptance_criteria>
  <done>page.tsx has 6 section stubs with locked IDs and aria-labels; no component imports yet</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Add OG image and resume PDF to public/</name>
  <files>
    harsha-portfolio/public/og-image.png,
    harsha-portfolio/public/resume.pdf
  </files>
  <action>
    Two files must be placed in /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio/public/:

    FILE 1: resume.pdf
    The resume PDF already exists at the project root. Copy it:
    ```bash
    cp "/Users/lakshmanturlapati/Documents/Codes/Harsha/Harsha Vardhini Veeravalli Prabu - Marketing Resume.pdf" \
       /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio/public/resume.pdf
    ```

    FILE 2: og-image.png (1200x630 pixels)
    Required for LinkedIn sharing large card preview (POL-02). layout.tsx references it at openGraph.images[0].url = '/og-image.png'.
    Specifications:
    - Dimensions: exactly 1200x630 pixels
    - Background: off-white (#FAFAFA)
    - Primary text: "Harsha Vardhini Veeravalli Prabu" in dark color (#1a1a2e), large font
    - Subtitle text: "Digital Marketing Professional" in medium gray, smaller font
    - No emojis
    Create using Canva (set custom dimensions 1200x630, export PNG) or Figma (frame at 1200x630, export PNG).
  </action>
  <verify>
    <automated>ls /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio/public/resume.pdf /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio/public/og-image.png 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - /harsha-portfolio/public/resume.pdf exists and is a valid PDF file
    - /harsha-portfolio/public/og-image.png exists and is a PNG file
    - `file public/og-image.png` returns "PNG image data" (not JPEG, not GIF)
  </acceptance_criteria>
  <done>Both public/og-image.png (1200x630 PNG) and public/resume.pdf exist in the public/ directory</done>

  <checkpoint>
    PAUSE: Place both files in harsha-portfolio/public/ as described above.

    After placing both files, confirm:
    1. `ls -la /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio/public/` shows both og-image.png and resume.pdf
    2. `file /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio/public/og-image.png` shows PNG image data
    3. `file /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio/public/resume.pdf` shows PDF document

    You can also start the dev server and visit http://localhost:3000 to confirm the page renders without errors -- a blank off-white page is the expected Phase 1 result.

    Type "done" when both files are in harsha-portfolio/public/.
    Type "skip-og" if you want to defer the OG image and only copy the resume PDF now.
  </checkpoint>
</task>

</tasks>

<verification>
After both tasks complete, run the TypeScript check to confirm page.tsx is valid:
```bash
cd /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio && npx tsc --noEmit 2>&1
```
Expected: zero errors.

Verify public assets:
```bash
ls /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio/public/
```
Expected: og-image.png and resume.pdf listed.

Confirm section IDs are locked:
```bash
grep "id=" /Users/lakshmanturlapati/Documents/Codes/Harsha/harsha-portfolio/src/app/page.tsx
```
Expected output includes all 6 section IDs: hero, about, experience, skills, leadership, contact.
</verification>

<success_criteria>
- page.tsx has exactly 6 section stubs with IDs: hero, about, experience, skills, leadership, contact
- All section IDs match the locked list (never change these after Phase 2 begins)
- public/resume.pdf exists (Harsha's marketing resume)
- public/og-image.png exists at 1200x630 (or placeholder noted for deferred completion)
- npx tsc --noEmit exits 0 across the full project
- Phase 1 is complete: running `npm run dev` would render a blank off-white page with Inter font and no console errors
</success_criteria>

<output>
After completion, create `/Users/lakshmanturlapati/Documents/Codes/Harsha/.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
