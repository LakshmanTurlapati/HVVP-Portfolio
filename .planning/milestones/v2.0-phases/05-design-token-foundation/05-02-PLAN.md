---
phase: 05-design-token-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - harsha-portfolio/src/app/layout.tsx
  - harsha-portfolio/src/components/sections/HeroSection.tsx
  - harsha-portfolio/src/components/sections/ContactSection.tsx
  - .planning/phases/05-design-token-foundation/05-01-ACCENT-AUDIT.md
autonomous: false
requirements:
  - BRAND-04
  - BRAND-05
  - BRAND-06
must_haves:
  truths:
    - "All 27 pre-existing accent-prefixed class usages are enumerated in an audit table with file, line, class, surface color, text color, new contrast ratio, and verdict"
    - "Every FAIL-verdict call site is rewritten to a compliant alternative and documented in the audit"
    - "Skip-link, when focused via keyboard Tab, renders with DeepBlack #0F0F0F background and White #F9F9F9 text at 4.5:1 contrast (not orange on white)"
    - "No white-on-orange button text remains in HeroSection or ContactSection -- labels are DeepBlack-on-Orange (6.68:1 PASS) or rewritten"
    - "Orange (#FF6A00) is not applied as body paragraph text anywhere in src/"
  artifacts:
    - path: ".planning/phases/05-design-token-foundation/05-01-ACCENT-AUDIT.md"
      provides: "Call-site-by-call-site audit of every accent-prefixed class in src/ with new-palette contrast verdict"
      contains: "file, line, class, surface, text, new-contrast, verdict"
      min_lines: 60
    - path: "harsha-portfolio/src/app/layout.tsx"
      provides: "Skip-link rewritten from focus:bg-accent focus:text-white to focus:bg-foreground focus:text-background"
      contains: "focus:bg-foreground focus:text-background"
  key_links:
    - from: ".planning/phases/05-design-token-foundation/05-01-ACCENT-AUDIT.md"
      to: "harsha-portfolio/src/components/sections/HeroSection.tsx line 26"
      via: "FAIL verdict triggers rewrite of bg-accent text-white -> bg-accent text-accent-foreground"
      pattern: "bg-accent text-accent-foreground"
    - from: ".planning/phases/05-design-token-foundation/05-01-ACCENT-AUDIT.md"
      to: "harsha-portfolio/src/components/sections/ContactSection.tsx line 25"
      via: "FAIL verdict triggers rewrite of bg-accent text-white -> bg-accent text-accent-foreground"
      pattern: "bg-accent text-accent-foreground"
    - from: "harsha-portfolio/src/app/layout.tsx line 92 skip-link"
      to: "@theme --color-foreground + --color-background tokens"
      via: "focus:bg-foreground focus:text-background utility classes"
      pattern: "focus:bg-foreground focus:text-background"
---

<objective>
Audit every pre-existing accent-prefixed class usage across src/ (27 usages across 9 files per prior grep), classify each as PASS (safe to keep under new orange token), FAIL (requires rewrite), or SAFE (no text, just border/fill -- Pitfall 1 irrelevant), rewrite every FAIL verdict to a WCAG 2.2 AA compliant alternative, and rewrite the skip-link from `focus:bg-accent focus:text-white` (2.87:1 FAIL under orange) to `focus:bg-foreground focus:text-background` (19:1 PASS).

Purpose: Plan 05-01 migrated --color-accent from muted blue to orange. That swap silently regresses WCAG 2.2 AA compliance shipped in v1.0 Phase 4 because white-on-orange (buttons) and orange-on-white (text near figcaption, metric numbers) now fall below 4.5:1 text and 3:1 UI thresholds. The skip-link is the most critical accessibility affordance and must be fixed first. This plan closes Pitfall 2 (token collision across 27 usages) by producing an audit doc + rewriting FAIL call sites.

Output: .planning/phases/05-design-token-foundation/05-01-ACCENT-AUDIT.md with the complete 27-row audit table, updated HeroSection.tsx + ContactSection.tsx button labels (white -> accent-foreground), updated layout.tsx skip-link. Any other FAIL-verdict usages discovered during audit are rewritten in the same commit. Commit: "feat(05): audit 27 accent usages and rewrite skip-link".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-design-token-foundation/05-CONTEXT.md
@.planning/phases/05-design-token-foundation/05-01-PLAN.md
@.planning/research/PITFALLS.md
@harsha-portfolio/src/app/layout.tsx
@harsha-portfolio/src/app/globals.css
@harsha-portfolio/src/components/layout/Navbar.tsx
@harsha-portfolio/src/components/sections/HeroSection.tsx
@harsha-portfolio/src/components/sections/ContactSection.tsx
@harsha-portfolio/src/components/sections/AboutSection.tsx
@harsha-portfolio/src/components/sections/ExperienceSection.tsx
@harsha-portfolio/src/components/sections/SkillsSection.tsx
@harsha-portfolio/src/components/sections/LeadershipSection.tsx

<interfaces>
<!-- Known accent-prefixed usages from prior grep on main branch (27 usages across 9 files) -->
<!-- These are the SEED list. Task 1 re-runs grep after plan 05-01 tokens ship to catch any drift. -->

From globals.css (3):
  line 14: --color-accent: oklch(0.55 0.10 240)                  [now oklch(0.7009 0.2012 44.77) after 05-01]
  line 15: --color-accent-hover: oklch(0.48 0.12 240)             [now var(--color-brand-orange-soft) after 05-01]
  line 43: outline: 2px solid var(--color-accent)                 [focus ring -- UI element, 3:1 threshold applies]

From layout.tsx (1):
  line 92: focus:bg-accent focus:text-white                        [skip-link -- FAIL under orange, MUST REWRITE per BRAND-05]

From Navbar.tsx (2):
  line 37: text-accent font-medium                                 [active nav link on white bg -- 2.73:1 FAIL body, but font-medium makes it display/UI-ish]
  line 73: text-accent font-medium                                 [active nav link mobile -- same concern]

From HeroSection.tsx (3):
  line 11: text-accent mb-4 tracking-wide                          [eyebrow/kicker on white bg -- small text FAIL for body 4.5:1]
  line 26: bg-accent text-white hover:bg-accent/90                 [CTA button -- white-on-orange 2.87:1 FAIL, MUST REWRITE]
  line 42: text-accent                                             [3xl/4xl metric numbers -- large text, 3:1 threshold, PASS]

From ExperienceSection.tsx (4):
  line 32: w-12 h-1 bg-accent                                      [underline rule -- non-text UI element, SAFE]
  line 44: bg-accent border-2 border-background ring-1 ring-accent/30  [timeline dot -- non-text UI, SAFE]
  line 50: text-sm font-medium text-accent                         [company name on white -- small text FAIL body]
  line 59: text-accent mt-1 shrink-0                               [dash/separator &ndash; -- decorative icon, SAFE]

From SkillsSection.tsx (2):
  line 15: w-12 h-1 bg-accent                                      [underline rule -- SAFE]
  line 63: border-l-2 border-accent                                [left border rule -- non-text, SAFE]

From LeadershipSection.tsx (3):
  line 13: w-12 h-1 bg-accent                                      [underline rule -- SAFE]
  line 22: bg-accent border-2 border-background ring-1 ring-accent/30  [timeline dot -- non-text UI, SAFE]
  line 25: text-sm font-medium text-accent                         [organization name on white -- small text FAIL body]

From ContactSection.tsx (2):
  line 16: w-12 h-1 bg-accent                                      [underline rule -- SAFE]
  line 25: bg-accent text-white hover:bg-accent/90                 [CTA button -- MUST REWRITE]

From AboutSection.tsx (3):
  line 10: w-12 h-1 bg-accent                                      [underline rule -- SAFE]
  line 24: border-l-2 border-accent                                [left border rule -- SAFE]
  line 32: text-xs font-medium text-accent                         [honors line -- tiny text FAIL body]

TOTAL: 3 + 1 + 2 + 3 + 4 + 2 + 3 + 2 + 3 = 23
<!-- User decision D-Audit specifies "27 pre-existing accent usages across 9 files". -->
<!-- Task 1 runs fresh grep to enumerate all 27 and reconcile any drift from this seed list. -->

<!-- WCAG contrast targets (computed from brand hex, confirmed in PITFALLS.md Pitfall 1) -->
<!-- #0F0F0F on #FF6A00 = 6.68:1 PASS (body + UI) -->
<!-- #0F0F0F on #F9F9F9 = ~19:1 PASS (body + AAA large) -->
<!-- #F9F9F9 on #0F0F0F = ~19:1 PASS -->
<!-- #FF6A00 on #F9F9F9 = 2.73:1 FAIL body (4.5), FAIL UI (3.0) -->
<!-- #FF6A00 on #FFFFFF = ~2.73:1 FAIL body, FAIL UI -->
<!-- #FFFFFF on #FF6A00 = 2.87:1 FAIL text -->
<!-- #FF6A00 on #0F0F0F = 6.68:1 PASS -->

<!-- Audit verdict classification rules -->
<!-- PASS: surface/text combo >= 4.5:1 for body text OR >= 3:1 for >=24px text or UI element -->
<!-- FAIL: surface/text combo fails both thresholds -> MUST REWRITE -->
<!-- SAFE: class is non-text (border, bg fill with no text child, decorative icon aria-hidden) -> no text threshold applies -->
<!-- Rewrite recipes: -->
<!--   bg-accent text-white  ->  bg-accent text-accent-foreground  (DeepBlack on orange, 6.68:1 PASS) -->
<!--   text-accent (small body text on white)  ->  text-foreground  OR  keep as-is if "large text" exception applies -->
<!--   focus:bg-accent focus:text-white  ->  focus:bg-foreground focus:text-background  (BRAND-05 exact rewrite) -->
</interfaces>

<!-- Pitfalls avoided in this plan -->
<!-- Pitfall 1: Orange contrast -- this plan closes Pitfall 1 by rewriting every FAIL-verdict usage -->
<!-- Pitfall 2: --color-accent token collision -- audit doc is the systematic fix -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate all accent-prefixed usages and write 05-01-ACCENT-AUDIT.md</name>
  <files>.planning/phases/05-design-token-foundation/05-01-ACCENT-AUDIT.md</files>
  <action>
  Run a fresh grep across harsha-portfolio/src/ to enumerate every accent-prefixed utility class usage. Use this grep pattern to catch all Tailwind accent variants:

  ```bash
  cd harsha-portfolio && grep -rn -E "(bg-accent|text-accent|border-accent|ring-accent|focus:bg-accent|focus:text-accent|hover:bg-accent|hover:text-accent|from-accent|to-accent|via-accent|outline-accent|accent-hover|accent-foreground)" src/ --include="*.tsx" --include="*.ts" --include="*.css"
  ```

  This will output roughly 27 lines. Reconcile against the seed list in the `<interfaces>` block of this plan's context. Expected deltas: there may be near-matches (e.g., `accent/30` for ring opacity) that the seed list already captured -- include them as individual rows.

  **Create the audit document** at `.planning/phases/05-design-token-foundation/05-01-ACCENT-AUDIT.md` with this exact structure:

  ```markdown
  # Phase 5 -- Accent Usage Audit

  **Generated:** YYYY-MM-DD (fill with today's date)
  **Phase:** 05-design-token-foundation
  **Closes:** BRAND-04, BRAND-06, Pitfall 2 (token collision)
  **Precondition:** Plan 05-01 complete -- --color-accent now resolves to oklch(0.7009 0.2012 44.77) = #FF6A00

  ## WCAG 2.2 AA Thresholds Applied

  - Body text (< 18px or < 14px bold): **4.5:1** minimum
  - Large text (>= 18px OR >= 14px bold): **3:1** minimum
  - Non-text UI element (border, icon, focus ring): **3:1** minimum
  - Decorative (aria-hidden, non-functional): no threshold

  ## Brand Palette Contrast Matrix (computed)

  | Foreground | Background | Ratio | Verdict |
  |------------|------------|-------|---------|
  | #0F0F0F DeepBlack | #F9F9F9 White | 19:1 | PASS body + AAA large |
  | #F9F9F9 White | #0F0F0F DeepBlack | 19:1 | PASS body + AAA large |
  | #FF6A00 Orange | #0F0F0F DeepBlack | 6.68:1 | PASS body + UI |
  | #0F0F0F DeepBlack | #FF6A00 Orange | 6.68:1 | PASS body + UI |
  | #FF6A00 Orange | #F9F9F9 White | 2.73:1 | FAIL body + FAIL UI |
  | #FFFFFF White | #FF6A00 Orange | 2.87:1 | FAIL body + FAIL UI |

  ## Audit Table

  | # | File | Line | Class | Surface | Text | Contrast | Verdict | Action |
  |---|------|------|-------|---------|------|----------|---------|--------|
  | 1 | src/app/globals.css | 14 | --color-accent definition | n/a | n/a | n/a | NOTE | token def, not a call site |
  | 2 | src/app/globals.css | 15 | --color-accent-hover definition | n/a | n/a | n/a | NOTE | token def, not a call site |
  | 3 | src/app/globals.css | 43 | outline: 2px solid var(--color-accent) | #F9F9F9 body | orange 2px outline | 2.73:1 on white | FAIL | Fix in Phase 10 (focus ring contrast against off-white bg fails 3:1 UI). Document here, defer to Phase 10 per D-Verification (automated contrast audit deferred). |
  | 4 | src/app/layout.tsx | 92 | focus:bg-accent focus:text-white | orange fill | white text | 2.87:1 | FAIL | REWRITE -> focus:bg-foreground focus:text-background (Task 2) |
  ...
  ```

  Continue the table for EVERY grep hit, one row per usage. For each row:
  - **File + Line**: exact grep output
  - **Class**: the accent-prefixed class(es) on that line
  - **Surface**: the background color the class lands against (infer from parent -- most usages are on off-white body bg F9F9F9; CTA buttons are orange fill themselves)
  - **Text**: the color of any text within the element (for bg-* classes) OR the element's own color (for text-* classes)
  - **Contrast**: computed ratio from brand palette matrix above
  - **Verdict**: one of PASS, FAIL, SAFE, NOTE (NOTE = token definition, not a call site)
  - **Action**: KEEP, REWRITE (with target class), or DEFER (for focus-ring-on-white which requires a different fix scope than text/CTA -- flag for Phase 10)

  **Classification guide** (apply systematically):
  - Class `bg-accent` WITH text child `text-white` -> FAIL -> REWRITE to `bg-accent text-accent-foreground`
  - Class `text-accent` on tiny body copy (text-xs, text-sm non-display) on white bg -> FAIL -> REWRITE to `text-foreground` OR flag for contextual review
  - Class `text-accent` on 3xl/4xl/2xl display numbers -> PASS (3:1 UI threshold applies for >=18px)
  - Class `bg-accent` as pure decorative fill (w-12 h-1 underline rules, timeline dots with no text) -> SAFE
  - Class `border-accent` / `ring-accent` as decorative border -> SAFE
  - Class `text-accent` inside `aria-hidden="true"` decorative element -> SAFE
  - Nav active-link `text-accent font-medium` on white bg (text-base ~16px) -> FAIL body, borderline UI -> REWRITE to `text-foreground underline underline-offset-4 decoration-accent` OR flag for Phase 6 nav rebuild. Document as FAIL + DEFER if nav is being rebuilt in Phase 9.

  **At the bottom of the audit doc**, add a summary section:

  ```markdown
  ## Summary

  - Total usages audited: {N}
  - PASS verdicts (keep as-is): {N}
  - FAIL verdicts rewritten in this plan: {N}
  - FAIL verdicts deferred to later phase (with phase reference): {N}
  - SAFE verdicts (non-text, no threshold): {N}
  - NOTE rows (token definitions): {N}

  ## Rewrites Applied in This Plan (05-02)

  1. src/app/layout.tsx:92 -- skip-link: `focus:bg-accent focus:text-white` -> `focus:bg-foreground focus:text-background` (BRAND-05)
  2. src/components/sections/HeroSection.tsx:26 -- CTA button: `bg-accent text-white hover:bg-accent/90` -> `bg-accent text-accent-foreground hover:bg-accent/90` (BRAND-04)
  3. src/components/sections/ContactSection.tsx:25 -- CTA button: `bg-accent text-white hover:bg-accent/90` -> `bg-accent text-accent-foreground hover:bg-accent/90` (BRAND-04)
  {additional FAIL-verdict rewrites from audit}

  ## Deferred to Later Phases

  | Usage | Deferred To | Reason |
  |-------|-------------|--------|
  | globals.css:43 focus ring on white bg | Phase 10 | Requires focus-ring strategy rethink (outline offset + darker accent on light surfaces); out of Phase 5 scope per D-Verification |
  | text-accent eyebrows/labels at text-sm on white bg | Phase 6 Content Rebuild | HeroSection/ExperienceSection/LeadershipSection/AboutSection are being rewritten in Phase 6 with brief-supplied copy; orange small text will be rewritten contextually then |
  | Navbar active-link text-accent | Phase 9 Nav Rewire | Navbar is being rebuilt in Phase 9 with new 7-item lineup; current active-link styling is obsolete |
  ```

  **Important:** The audit doc must list ALL usages from the grep, not just the ones being rewritten in this plan. The audit doc itself is the deliverable for BRAND-06. Rewrites are the deliverable for BRAND-04 + BRAND-05.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-design-token-foundation/05-01-ACCENT-AUDIT.md && echo "PASS: audit file exists" || { echo "FAIL: audit file missing"; exit 1; }</automated>
    <automated>wc -l .planning/phases/05-design-token-foundation/05-01-ACCENT-AUDIT.md | awk '{ if ($1 >= 60) print "PASS: audit file has " $1 " lines"; else { print "FAIL: audit too short, lines=" $1; exit 1 } }'</automated>
    <automated>grep -E "^\| [0-9]+ \| " .planning/phases/05-design-token-foundation/05-01-ACCENT-AUDIT.md | wc -l | awk '{ if ($1 >= 20) print "PASS: audit has " $1 " usage rows"; else { print "FAIL: too few audit rows=" $1; exit 1 } }'</automated>
    <automated>grep -E "FAIL|PASS|SAFE|NOTE" .planning/phases/05-design-token-foundation/05-01-ACCENT-AUDIT.md | wc -l | awk '{ if ($1 >= 20) print "PASS: verdicts assigned"; else { print "FAIL: missing verdicts, count=" $1; exit 1 } }'</automated>
    <automated>grep -E "## Summary" .planning/phases/05-design-token-foundation/05-01-ACCENT-AUDIT.md | wc -l | awk '{ if ($1 == 1) print "PASS: summary section present"; else { print "FAIL: summary missing"; exit 1 } }'</automated>
    <automated>grep -E "## Rewrites Applied in This Plan" .planning/phases/05-design-token-foundation/05-01-ACCENT-AUDIT.md | wc -l | awk '{ if ($1 == 1) print "PASS: rewrites section present"; else { print "FAIL: rewrites section missing"; exit 1 } }'</automated>
  </verify>
  <done>
    05-01-ACCENT-AUDIT.md exists with a header section, WCAG thresholds explanation, brand palette contrast matrix, one table row per accent-prefixed usage in src/ (>= 20 rows, targeting all ~27 from seed list + any grep drift), each row has file/line/class/surface/text/contrast/verdict/action columns filled, summary section counts verdicts, rewrites section lists call sites being fixed in this plan, deferred section lists call sites flagged for Phase 6/9/10.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite skip-link from accent-on-white to foreground-on-background (BRAND-05)</name>
  <files>harsha-portfolio/src/app/layout.tsx</files>
  <action>
  Rewrite the skip-link in harsha-portfolio/src/app/layout.tsx line 92 to use DeepBlack background and White text (the 19:1 combo) instead of orange background and white text (the 2.87:1 FAIL combo).

  **Current** (line 92):
  ```tsx
  className="sr-only focus:not-sr-only focus:fixed focus:top-4 focus:left-4 focus:z-[100] focus:px-4 focus:py-2 focus:bg-accent focus:text-white focus:rounded-md focus:text-sm focus:font-medium"
  ```

  **Change to** (per user decision D-Skip-Link, exact rewrite from CONTEXT.md):
  ```tsx
  className="sr-only focus:not-sr-only focus:fixed focus:top-4 focus:left-4 focus:z-[100] focus:px-4 focus:py-2 focus:bg-foreground focus:text-background focus:rounded-md focus:text-sm focus:font-medium"
  ```

  **Exact diff:**
  - `focus:bg-accent` -> `focus:bg-foreground` (orange #FF6A00 -> DeepBlack #0F0F0F)
  - `focus:text-white` -> `focus:text-background` (#FFFFFF -> off-white #F9F9F9)

  This produces 19:1 contrast (DeepBlack on off-white) which passes WCAG 2.2 AA body (4.5:1) with massive headroom.

  **Why NOT `focus:bg-foreground focus:text-white`:** the pure-white text constant #FFFFFF is still accessible here (19:1 on DeepBlack), but `focus:text-background` uses the semantic token which binds to --color-brand-white #F9F9F9. This keeps the skip-link within the brand palette and still gives 18.7:1 (well above 4.5:1). Using semantic tokens means if the palette ever darkens the background further, the skip-link text automatically tracks with it.

  Keep all other classes (sr-only, focus:not-sr-only, focus:fixed, positioning, padding, z-index, border-radius, text-sm, font-medium) unchanged.

  Do NOT modify any other part of layout.tsx. The Montserrat font import from plan 05-01 stays. The metadata, jsonLd, and html/body/header/main/footer structure stay.
  </action>
  <verify>
    <automated>cd harsha-portfolio && grep -E "focus:bg-foreground focus:text-background" src/app/layout.tsx | wc -l | awk '{ if ($1 == 1) print "PASS: skip-link rewritten"; else { print "FAIL: new skip-link classes not found"; exit 1 } }'</automated>
    <automated>cd harsha-portfolio && grep -E "focus:bg-accent focus:text-white" src/app/layout.tsx | wc -l | awk '{ if ($1 == 0) print "PASS: old skip-link classes removed"; else { print "FAIL: old skip-link classes still present"; exit 1 } }'</automated>
    <automated>cd harsha-portfolio && grep -E "Skip to main content" src/app/layout.tsx | wc -l | awk '{ if ($1 == 1) print "PASS: skip-link text preserved"; else { print "FAIL: skip-link text missing or duplicated"; exit 1 } }'</automated>
  </verify>
  <done>
    layout.tsx line 92 skip-link className replaces `focus:bg-accent focus:text-white` with `focus:bg-foreground focus:text-background`. All other skip-link classes (sr-only, focus positioning, padding, rounded, text-sm, font-medium) preserved. "Skip to main content" anchor text preserved. BRAND-05 requirement satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite white-on-orange button labels to DeepBlack-on-orange (BRAND-04)</name>
  <files>harsha-portfolio/src/components/sections/HeroSection.tsx, harsha-portfolio/src/components/sections/ContactSection.tsx</files>
  <action>
  Rewrite every `bg-accent text-white` class pair to `bg-accent text-accent-foreground` in CTA buttons. The `--color-accent-foreground` token was created in plan 05-01 task 1 and resolves to `var(--color-brand-black)` = DeepBlack #0F0F0F.

  **HeroSection.tsx line 26** (primary CTA -- "Get In Touch" or similar):

  Current:
  ```tsx
  className={cn(buttonVariants({ size: 'lg' }), 'bg-accent text-white hover:bg-accent/90')}
  ```

  Change to:
  ```tsx
  className={cn(buttonVariants({ size: 'lg' }), 'bg-accent text-accent-foreground hover:bg-accent/90')}
  ```

  **ContactSection.tsx line 25** (email CTA):

  Current:
  ```tsx
  className={cn(buttonVariants({ size: 'lg' }), 'bg-accent text-white hover:bg-accent/90')}
  ```

  Change to:
  ```tsx
  className={cn(buttonVariants({ size: 'lg' }), 'bg-accent text-accent-foreground hover:bg-accent/90')}
  ```

  **Exact diff (both files):** `text-white` -> `text-accent-foreground`. Everything else on the line is preserved.

  **Why this works:**
  - White on orange = 2.87:1 FAIL (Pitfall 1, would have regressed v1.0 WCAG AA the moment plan 05-01 shipped)
  - DeepBlack on orange = 6.68:1 PASS (body text + UI thresholds, per PITFALLS.md Pitfall 1)
  - The accent-foreground token pair enforces this rule semantically across the codebase

  **If the audit in Task 1 identifies OTHER FAIL-verdict usages** beyond these two CTAs (e.g., a third CTA was added between audit and this plan, or there's a less-obvious white-on-orange combo), rewrite them in this task too using the same `text-white -> text-accent-foreground` pattern. Document each additional rewrite in the audit doc's "Rewrites Applied" section.

  Do NOT touch:
  - `text-accent` usages (those are separate concern -- see Deferred section of audit)
  - `bg-accent` decorative rules (w-12 h-1) -- SAFE verdict, no text inside
  - `border-accent` / `ring-accent` usages -- SAFE verdict
  - Buttons in files not being touched (unless audit flagged them as FAIL, in which case add them to this task's files list)

  Do NOT rename the token `--color-accent` itself. Per D-Accent-Naming, the token name stays `--color-accent` to minimize diff and preserve shadcn alignment.
  </action>
  <verify>
    <automated>cd harsha-portfolio && grep -rE "bg-accent text-white" src/ --include="*.tsx" | wc -l | awk '{ if ($1 == 0) print "PASS: no white-on-orange buttons remain"; else { print "FAIL: " $1 " white-on-orange button(s) still present"; exit 1 } }'</automated>
    <automated>cd harsha-portfolio && grep -rE "bg-accent text-accent-foreground" src/ --include="*.tsx" | wc -l | awk '{ if ($1 >= 2) print "PASS: " $1 " CTAs use accent-foreground"; else { print "FAIL: expected >=2 CTAs, found=" $1; exit 1 } }'</automated>
    <automated>cd harsha-portfolio && grep -E "bg-accent text-accent-foreground hover:bg-accent/90" src/components/sections/HeroSection.tsx | wc -l | awk '{ if ($1 == 1) print "PASS: HeroSection CTA rewritten"; else { print "FAIL: HeroSection CTA not rewritten"; exit 1 } }'</automated>
    <automated>cd harsha-portfolio && grep -E "bg-accent text-accent-foreground hover:bg-accent/90" src/components/sections/ContactSection.tsx | wc -l | awk '{ if ($1 == 1) print "PASS: ContactSection CTA rewritten"; else { print "FAIL: ContactSection CTA not rewritten"; exit 1 } }'</automated>
    <automated>cd harsha-portfolio && npx tsc --noEmit -p . 2>&1 | grep -E "(Section.tsx|error)" | head -5</automated>
  </verify>
  <done>
    HeroSection.tsx line 26 and ContactSection.tsx line 25 both replace `text-white` with `text-accent-foreground` in their `bg-accent` CTA button classNames. Zero instances of `bg-accent text-white` remain anywhere in src/ (grep returns 0 hits). Any additional FAIL-verdict usages flagged in Task 1 audit are also rewritten in the same pattern. TypeScript compiles without errors. BRAND-04 requirement (orange never appears with white-on-orange labels) satisfied.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification checkpoint -- audit completeness + skip-link + CTA rewrites</name>
  <files>n/a (verification checkpoint, touches nothing)</files>
  <action>
    **Checkpoint purpose:** Pause execution so the user can verify the audit doc is complete AND keyboard Tab reveals the rewritten skip-link AND CTA buttons render with DeepBlack-on-orange labels, before committing. This is a verification gate, not an implementation task.

    **What was automated (by Tasks 1, 2, 3 above):**
    05-01-ACCENT-AUDIT.md created with audit table (>=20 rows), brand palette contrast matrix, summary section, Rewrites Applied section, Deferred to Later Phases section. Skip-link in layout.tsx rewritten from `focus:bg-accent focus:text-white` to `focus:bg-foreground focus:text-background`. HeroSection.tsx + ContactSection.tsx CTA buttons rewritten from `bg-accent text-white` to `bg-accent text-accent-foreground`.

    Claude must start the dev server (if not already running), open the audit file for review, surface the verification steps below to the user, then wait for explicit approval before proceeding to the commit task.
  </action>
  <verify>
    <automated>echo "MANUAL VERIFICATION CHECKPOINT -- see <how-to-verify> block. Requires user to type approved."</automated>
  </verify>
  <done>User has typed "approved" after verifying audit completeness, skip-link focus state (Tab key), CTA button colors, and no undocumented orange body text.</done>
  <what-built>
    Plan 05-02 has produced a 27-row (or more) accent-usage audit documenting every bg-accent/text-accent/border-accent/ring-accent call site with a PASS/FAIL/SAFE/NOTE verdict, rewritten the keyboard skip-link from orange-on-white (2.87:1 FAIL) to DeepBlack-on-off-white (19:1 PASS), and rewritten the two CTA buttons (Hero primary + Contact email) from white-on-orange labels (2.87:1 FAIL) to DeepBlack-on-orange labels (6.68:1 PASS).
  </what-built>
  <how-to-verify>
    Visual + keyboard verification of all five phase success criteria.

    1. **Review the audit doc** at `.planning/phases/05-design-token-foundation/05-01-ACCENT-AUDIT.md`:
       - Open the file and scan the audit table.
       - Confirm every accent-prefixed usage you can find by grep is present as a row.
       - Confirm every row has a verdict column filled (not blank).
       - Confirm the Summary section counts add up (PASS + FAIL + SAFE + NOTE = total rows).
       - Confirm the Rewrites Applied section lists at least the 3 rewrites done in this plan (skip-link + 2 CTAs).

    2. **Start the dev server** (if not already running from plan 05-01):
       ```
       cd harsha-portfolio && npm run dev
       ```
       Open http://localhost:3000.

    3. **Verify skip-link focus state (Criterion 3)** -- this is the most critical test:
       - Click anywhere on the page to clear focus.
       - Press the Tab key ONCE.
       - Expected: A button labeled "Skip to main content" should appear in the top-left corner at fixed position top-4 left-4.
       - It must render with DeepBlack #0F0F0F background and off-white #F9F9F9 text.
       - It must NOT render with orange background (pre-rewrite appearance).
       - Contrast should be clearly extreme (near-black on near-white = ~19:1).

    4. **Verify CTA buttons (Criterion 5)** -- no white-on-orange labels anywhere:
       - Scroll to the Hero section. The primary CTA button should have orange background with DeepBlack text (not white text).
       - Scroll to the Contact section. The email CTA button should have orange background with DeepBlack text (not white text).
       - Button text should be clearly readable against the orange fill (6.68:1).

    5. **Verify no orange body paragraphs (Criterion 5)** -- spot-check:
       - Scan hero paragraph copy, about paragraph, experience descriptions -- body text should be DeepBlack on off-white, NOT orange.
       - The eyebrow/kicker above the hero name ("Available for full-time roles" or similar) may still be orange text-sm -- this is a KNOWN FAIL documented in the audit's "Deferred to Later Phases" section (rewritten in Phase 6 Content Rebuild).
       - If any orange body paragraph appears that is NOT documented as deferred in the audit, block and report.

    6. **Criterion 4 -- audit completeness** (final check):
       - Confirm the audit file counts align with the grep you can run yourself:
         ```
         cd harsha-portfolio && grep -rnE "(bg-accent|text-accent|border-accent|ring-accent|focus:bg-accent|focus:text-accent|accent-foreground|accent-hover)" src/ --include="*.tsx" --include="*.ts" --include="*.css" | wc -l
         ```
       - The row count in the audit table should match or exceed this number (audit may include the NOTE token-definition rows that grep also catches).

    7. **Confirm zero layout regression** -- scroll the full page, compare to the post-05-01 state. Only the skip-link focused state and CTA button text color should differ.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to commit, or describe issues (e.g. "skip-link still orange", "found undocumented white-on-orange button in footer", "audit missing Navbar.tsx rows")</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Commit plan 05-02 changes atomically</name>
  <files>.planning/phases/05-design-token-foundation/05-01-ACCENT-AUDIT.md, harsha-portfolio/src/app/layout.tsx, harsha-portfolio/src/components/sections/HeroSection.tsx, harsha-portfolio/src/components/sections/ContactSection.tsx</files>
  <action>
  After the checkpoint is approved, create the second atomic commit for phase 5 per user decision D-Commit-Strategy. This commit bundles the audit doc with every rewrite it justifies -- one logical unit.

  Run:
  ```bash
  node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "feat(05): audit 27 accent usages and rewrite skip-link" --files .planning/phases/05-design-token-foundation/05-01-ACCENT-AUDIT.md harsha-portfolio/src/app/layout.tsx harsha-portfolio/src/components/sections/HeroSection.tsx harsha-portfolio/src/components/sections/ContactSection.tsx
  ```

  **If Task 1's audit identified additional FAIL-verdict rewrites beyond the seed two CTAs + skip-link** (e.g., a stray white-on-orange usage in a file not anticipated), add those files to the --files list. The commit should contain the audit doc AND every file that was modified as a direct consequence of a FAIL verdict in that audit.

  Do NOT include:
  - globals.css or layout.tsx Montserrat code (already committed in plan 05-01)
  - Any section file with only deferred-to-phase-6 issues (those are not touched in this plan)
  - Unrelated changes
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Harsha && git log -1 --format='%s' | grep -E "feat\(05\): audit 27 accent usages and rewrite skip-link" && echo "PASS: commit created" || { echo "FAIL: expected commit not found"; exit 1; }</automated>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Harsha && git show --stat HEAD | grep -E "05-01-ACCENT-AUDIT.md" | wc -l | awk '{ if ($1 == 1) print "PASS: audit doc in commit"; else { print "FAIL: audit doc not in commit"; exit 1 } }'</automated>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Harsha && git show --stat HEAD | grep -E "(layout.tsx|HeroSection.tsx|ContactSection.tsx)" | wc -l | awk '{ if ($1 >= 3) print "PASS: rewrites in commit"; else { print "FAIL: missing rewrite files, count=" $1; exit 1 } }'</automated>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Harsha && git status --porcelain | grep -E "(layout.tsx|HeroSection.tsx|ContactSection.tsx|05-01-ACCENT-AUDIT.md)" | wc -l | awk '{ if ($1 == 0) print "PASS: no uncommitted changes to plan files"; else { print "FAIL: uncommitted changes remain"; exit 1 } }'</automated>
  </verify>
  <done>
    Single git commit "feat(05): audit 27 accent usages and rewrite skip-link" contains the audit doc + layout.tsx + HeroSection.tsx + ContactSection.tsx (plus any additional files from audit-driven FAIL rewrites). No uncommitted changes to those files remain. Commit 2 of 2 for phase 5.
  </done>
</task>

</tasks>

<verification>
Phase success criteria addressed by this plan (criteria 3, 4, 5 completed here; criteria 1 and 2 were completed in plan 05-01):

- Criterion 3 (skip-link DeepBlack bg + White text, 4.5:1 pass): verified via checkpoint Task 4 step 3 -- keyboard Tab reveals DeepBlack-on-off-white skip-link (19:1, vastly exceeds 4.5:1).
- Criterion 4 (all 27 accent usages audited, each PASS or rewritten): verified via checkpoint Task 4 step 6 -- audit file row count matches grep count, every row has a verdict.
- Criterion 5 (orange never as body text or white-on-orange buttons): verified via checkpoint Task 4 steps 4 + 5 -- grep confirms zero `bg-accent text-white`, visual walkthrough confirms no undocumented orange body paragraphs.
- BRAND-04 (orange restricted to DeepBlack surfaces / large UI, never body / white-on-orange): completed in Task 3 (CTA rewrites) + audit documents the boundary.
- BRAND-05 (skip-link rewritten exact string): completed in Task 2.
- BRAND-06 (all 27 usages audited call-site-by-call-site with contrast verification): completed in Task 1 (audit doc is the deliverable).

Closes Pitfall 1 (orange contrast failure) for button labels + skip-link. Closes Pitfall 2 (token collision across 27 usages) via systematic audit + rewrite. Deferred concerns (focus ring contrast on white bg, text-accent on small body copy in soon-to-be-rebuilt sections) are documented in the audit's "Deferred to Later Phases" table with phase references.
</verification>

<success_criteria>
- .planning/phases/05-design-token-foundation/05-01-ACCENT-AUDIT.md exists with >= 20 usage rows (targeting all ~27 accent-prefixed call sites in src/), each row has file/line/class/surface/text/contrast/verdict/action columns filled
- Audit doc has WCAG thresholds section, brand palette contrast matrix, summary section counting PASS/FAIL/SAFE/NOTE, Rewrites Applied section, Deferred to Later Phases section
- harsha-portfolio/src/app/layout.tsx skip-link className changed from `focus:bg-accent focus:text-white` to `focus:bg-foreground focus:text-background` (BRAND-05 exact rewrite)
- harsha-portfolio/src/components/sections/HeroSection.tsx line 26 and ContactSection.tsx line 25 CTA buttons use `text-accent-foreground` instead of `text-white` (BRAND-04)
- grep of src/ for `bg-accent text-white` returns zero matches
- Keyboard Tab reveals skip-link with DeepBlack bg + off-white text (visible 19:1 contrast)
- TypeScript compiles without errors
- Single atomic commit "feat(05): audit 27 accent usages and rewrite skip-link" bundles the audit doc + all rewritten files
</success_criteria>

<output>
After completion, create `.planning/phases/05-design-token-foundation/05-02-SUMMARY.md` using the summary template. Include:
- Final audit row count (PASS / FAIL / SAFE / NOTE breakdown)
- List of all files rewritten in this plan (seed two CTAs + skip-link + any audit-driven additions)
- Deferred issues count + which later phases they target (Phase 6 content rebuild, Phase 9 nav rewire, Phase 10 polish)
- Commit hash
- Statement that Phase 5 is complete (both plans shipped) and Phase 6 (Content Sections Rebuild) is unblocked
</output>